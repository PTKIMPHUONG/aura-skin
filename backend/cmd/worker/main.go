package main

import (
	// "context"
	// "../config"
	// "../routes"
	// "log"
	// "os"
	// "os/signal"
	// "syscall"

	// "github.com/gofiber/fiber/v2"
)

func main() {
	// app := fiber.New()
	// config.ConnectDB()      // Kết nối đến MongoDB
	// routes.SetupRoutes(app) // Thiết lập các route
	// go func() {
	// 	if err := app.Listen(":3000"); err != nil {
	// 		log.Fatal(err)
	// 	}
	// }()

	// // Chờ tín hiệu dừng
	// ch := make(chan os.Signal, 1)
	// signal.Notify(ch, syscall.SIGINT, syscall.SIGTERM)
	// <-ch

	// // Ngắt kết nối MongoDB
	// if err := config.DB.Disconnect(context.Background()); err != nil {
	// 	log.Fatal(err)
	// }
	// log.Println("Disconnected from MongoDB!")
}
